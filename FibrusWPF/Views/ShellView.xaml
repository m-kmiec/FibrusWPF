<Window x:Class="FibrusWPF.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FibrusWPF.Views"
        mc:Ignorable="d" FontSize="20"
        Title="ShellView" Height="600" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <DataGrid x:Name="Students"  AlternatingRowBackground="Gray"
                  AutoGenerateColumns="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Number" Binding="{Binding Path=Id}"></DataGridTextColumn>
                <DataGridTextColumn Header="Full name" Binding="{Binding Path=Personals}"></DataGridTextColumn>
                <DataGridTextColumn Header="Marks" Binding="{Binding Path=MarkString}"></DataGridTextColumn>
                <DataGridTextColumn Header="Average grade" Binding="{Binding Path=AverageGrade}"></DataGridTextColumn>
                <DataGridTextColumn Header="Marks list" Binding="{Binding Path=Marks}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>